

export default function LayoutAuth({ children}: { children: React.ReactElement})  {
  return (
    <div className="flex flex-col justify-center h-full items-center">
       
        {children}
    </div>
  )
}

